# Database SQLite locale (file creato automaticamente da Prisma)
DATABASE_URL="file:./dev.db"

# Chiave API Anthropic (Claude), usata sia per l'estrazione di entita'/
# sentiment (Haiku) sia per le spiegazioni didattiche (Sonnet)
ANTHROPIC_API_KEY=""
